project Main is
   for Source_Dirs use ("src");
   for Main use ("main.adb");
   for Object_Dir use ".objs";

   package Compiler is
      for Default_Switches ("Ada") use ("-g");
   end Compiler;
end Main;